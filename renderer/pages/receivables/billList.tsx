import React from 'react'
import { BillListContainer } from '../../features/receivables/container/BillListContainer'

const billList = ({dispatch, router}) => <BillListContainer dispatch={dispatch} router={router}/>

export default billList